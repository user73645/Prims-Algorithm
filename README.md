To compile on Windows, assuming MinGW is installed:
$ g++ main.cpp prim.cpp -o prim.exe

On unix based system, assuming GCC is installed:
$ g++ main.cpp prim.cpp -o prim

Usage:
Once compiled, start the executable by running:
./<executable> PATH/TO/TESTDATA
